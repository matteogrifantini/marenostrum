name: Forecast Sync

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  sync-forecasts:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Forecast Sync Endpoint
        run: |
          curl --fail --show-error --retry 5 --retry-delay 10 --retry-all-errors \
            -X GET "https://marenostrum.app/api/cron/forecast" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json"
